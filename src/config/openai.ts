// OpenAI API key should be stored in environment variables
// This is a placeholder - in production, use environment variables or a secure key management system
export const OPENAI_API_KEY = process.env.OPENAI_API_KEY || '';

// For development, add your key to .env file and use react-native-dotenv
// import { OPENAI_API_KEY } from '@env';
